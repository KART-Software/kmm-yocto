# NFS rootfs ブート (bring-up 専用 — kas/imx8mm-netboot.yml が SRC_URI に
# 追加する。slim-imx.cfg の NFS 削除より後にマージされるので勝つ)
CONFIG_NFS_FS=y
CONFIG_NFS_V3=y
CONFIG_ROOT_NFS=y
# カーネル自身の IP 自動設定 (ip=dhcp)
CONFIG_IP_PNP=y
CONFIG_IP_PNP_DHCP=y
