# Cortex-M4 を Linux から起動/停止するための remoteproc + rpmsg。
# imx_rproc は DT の fsl,imx8mm-cm4 ノードがある機体 (xpi) でのみ probe。
# rpmsg 系は将来の A53<->M4 通信用 (ドライバは =m でロード時のみ有効)。
CONFIG_IMX_REMOTEPROC=y
CONFIG_RPMSG_VIRTIO=y
CONFIG_RPMSG_CHAR=m
CONFIG_RPMSG_CTRL=m
CONFIG_RPMSG_TTY=m
