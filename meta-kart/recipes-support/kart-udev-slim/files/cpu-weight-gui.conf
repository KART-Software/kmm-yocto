# GUI クリティカルチェーン (seatd/weston/kmm) の起動時 CPU 配分を優遇する。
# basic.target 直後のスタート集中では 4 コアを 10 数サービスが取り合い、
# seatd の起動 (本来 ~0ms) が 0.6〜0.7s 遅れる実測がある。
# StartupCPUWeight は systemd が「起動フェーズ」と判定している間だけ有効で、
# 定常状態には影響しない (既定 weight は 100)。
[Service]
StartupCPUWeight=500
