# dbus 本体を GUI (kmm READY) 後へ退避する (513ms を起動嵐から除去)。
# 実機調査ではバスの客は systemd ファミリーのみ (weston/kmm/tailscaled は
# 不使用)。dbus.socket は先に立つので、早期に接続したクライアント
# (networkd 等) はカーネルのソケットバッファで待ち、daemon 起動後に
# ハンドシェイクされる。resolved/timesyncd は元々 kmm 後へ遅延済み。
[Unit]
After=kmm.service
