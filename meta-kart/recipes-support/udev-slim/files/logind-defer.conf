# logind を GUI (kmm READY) 後へ退避する。
# logind の唯一の実需要は ssh ログインのセッション登録 (Tailscale SSH →
# PAM → logind → user@N。実機で確認) で、起動嵐の中で走る必要が無い。
# GUI 表示前に ssh すると、セッション登録がここまで数秒待たされるだけ。
[Unit]
After=kmm.service
