header:
  version: 14
  includes:
    - base.yml
    - rpi5.yml

target: kart-image

local_conf_header:
  prod-features: |
    IMAGE_ROOTFS_EXTRA_SPACE = "0"
    EXTRA_IMAGE_FEATURES:append = " ssh-server-openssh"
