header:
  version: 14

local_conf_header:
  boot-nvme: |
    WKS_FILE = "kart-rpi5-nvme-ab.wks"
    IMAGE_LINK_NAME = "${IMAGE_BASENAME}-${MACHINE}-nvme"
    CMDLINE_ROOT_PARTITION = "/dev/nvme0n1p5"
