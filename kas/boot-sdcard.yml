header:
  version: 14

local_conf_header:
  boot-sdcard: |
    WKS_FILE = "kart-rpi5-sdcard-ab.wks"
    IMAGE_LINK_NAME = "${IMAGE_BASENAME}-${MACHINE}-sdcard"
    CMDLINE_ROOT_PARTITION = "/dev/mmcblk0p5"
